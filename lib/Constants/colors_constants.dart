import 'dart:ui';

class ColorsConstants{
  static const background1 = Color.fromRGBO(251, 220, 225, 1); // Second `const` is optional in assignments.
  static const background2 = Color.fromRGBO(251, 241, 242, 1); // Second `const` is optional in assignments.
  static const background3 = Color(0xFFF2F2F2); // Second `const` is optional in assignments.
  static const background4 = Color(0xFFE6E6E6); // Second `const` is optional in assignments.
  static const background5 = Color(0xFFD7D7D7); // Second `const` is optional in assignments.
  static const background6 = Color(0xFFC8C8C8); // Second `const` is optional in assignments.
  static const background7 = Color(0xFFB4B4B4); // Second `const` is optional in assignments.

  static const chordsRed = Color(0xFFe3342f);
  static const chordsGreen = Color(0xFF38c172);
  static const chordsOrange = Color(0xFFcc6633);
  static const chordsDarkBlue = Color(0xFF00008B);
  static const chordsCyan = Color(0xFF018786);
  static const lightOrange = Color.fromRGBO(225, 162, 126, 1);
  static const orange = Color.fromRGBO(227, 117, 37, 1);
}